import 'package:flutter/material.dart';

class PairDeviceScreen extends StatelessWidget {
  const PairDeviceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pair Smart Device'),
        backgroundColor: const Color(0xFFF36060),
      ),
      body: const Center(
        child: Text('Pair Device Screen - Coming Soon!'),
      ),
    );
  }
}
